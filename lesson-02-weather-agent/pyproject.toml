[project]
name = "lesson-02-weather-agent"
version = "0.1.0"
description = "Lesson 02 — Tool use and function calling with the Anthropic API"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
    "httpx>=0.28.0",
]

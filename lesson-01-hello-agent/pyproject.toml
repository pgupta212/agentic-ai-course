[project]
name = "lesson-01-hello-agent"
version = "0.1.0"
description = "Lesson 01 — Streaming CLI chatbot with the Anthropic API"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
]
